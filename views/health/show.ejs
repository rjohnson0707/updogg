<%- include('../partials/header') %>

<section id="sections">
    <div>Name: <%= health.name %></div>
    <div>Address: <%= health.address %></div>
    <div>Description: <%= health.description %></div>
</section>

<a href="/health">Back to Locations</a>

<a href="/health/<%= health._id %>/edit">Edit Location?</a>
<form action="/health/<%= health._id %>?_method=DELETE" method="POST">
  <button type="submit">DELETE PARK</button>
  </form>

<br><br><h2>Reviews</h2>
<form method="POST"
  action="/health/<%= health._id %>/reviews">
  <label>Review:</label>
  <textarea name="content"></textarea>
  <label>Rating:</label>
  <select name="rating">
    <option value="1">1</option>
    <option value="2">2</option>
    <option value="3">3</option>
    <option value="4">4</option>
    <option value="5">5</option>
  </select>
  <input type="submit" value="Add Review">
</form>

<% if (health.reviews.length) { %>
  <table>
    <thead>
      <tr>
      	 <th>Date</th>
        <th>Review</th>
        <th>Rating</th>
      </tr>
    </thead>
    <tbody>
      <% var total = 0 %>
      <% health.reviews.forEach(function(h) { %>
        <% total += h.rating %>
        <tr>
          <td><%= h.createdAt.toLocaleDateString() %></td>
          <td><%= h.content %></td>
          <td><%= h.rating %></td>
        </tr>
        <% }); %>
        <tr>
          <td colspan="2"></td>
          <td><strong><%= (total / health.reviews.length).toFixed(1) %></strong></td>
        </tr>
    </tbody>
  </table>
<% } else { %>
  <h5>No Reviews Yet</h5>
<% } %>

<%- include('../partials/footer') %>