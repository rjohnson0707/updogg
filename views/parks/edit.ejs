<%- include('../partials/header'); %>

<section id="sections">
  <section id="centerSection">
    <h2>Edit <%= park.name %></h2>
    <form action="/parks/<%= park._id %>?_method=PUT" method="POST">
        <label>Name:
          <input type="text" name="name" value="<%= park.name %>">
        </label><br>
        <label>Address(City, State):
          <input type="text" name="address" value="<%= park.address %>">
        </label><br>
        <label>Description:
            <textarea name="description"><%= park.description %></textarea>
        </label><br>
        <label>Leash:
          <select name="leash">
            <option value="OnLeash">On-Leash</option>
            <option value="OffLeash">Off-Leash</option>
          </select>
        </label><br>
        <label>Attach Website URL:
          <input type="text" name="site" value="<%= park.site %>">
        </label>
        <input type="submit" value="Update">
    </form>
    <a href="/parks">Back to Parks</a>
</section>
</section>
    
<%- include('../partials/footer'); %>